interface AcceptButtonProps {
  onClick: () => void;
}

export function AcceptButton({ onClick }: AcceptButtonProps) {
  return (
    <button
      onClick={onClick}
      className="w-full px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition-colors"
    >
      Xác nhận
    </button>
  );
}
