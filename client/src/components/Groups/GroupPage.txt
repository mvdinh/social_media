import { useState } from 'react';
import  SidebarGroup from './SidebarGroup';
import { GroupFeed } from './GroupFeed';

export interface Group {
  id: string;
  name: string;
  description: string;
  image: string;
  memberCount: number;
  lastActive: string;
  isJoined: boolean;
}

function GroupPage() {
  const [selectedGroup, setSelectedGroup] = useState<Group | null>(null);
  const [isCreateModalOpen, setIsCreateModalOpen] = useState(false);
  const [groups, setGroups] = useState<Group[]>([
    {
      id: '1',
      name: 'Dev ơi mình đi đâu thế?',
      description: 'Cộng đồng những người yêu thích code và đi du lịch',
      image: 'https://images.unsplash.com/photo-1522071820081-009f0129c71c?w=400&h=400&fit=crop',
      memberCount: 168700,
      lastActive: 'Lần hoạt động gần nhất: 17 giờ trước',
      isJoined: true
    },
    {
      id: '2',
      name: 'Đại Học Dũng Học Đại',
      description: 'Nhóm học tập và chia sẻ kiến thức',
      image: 'https://images.unsplash.com/photo-1523240795612-9a054b0db644?w=400&h=400&fit=crop',
      memberCount: 45200,
      lastActive: 'Lần hoạt động gần nhất: 7 giờ trước',
      isJoined: true
    },
    {
      id: '3',
      name: 'Pass đồ sinh viên giá rẻ Hà Nội',
      description: 'Mua bán đồ cũ sinh viên',
      image: 'https://images.unsplash.com/photo-1556740758-90de374c12ad?w=400&h=400&fit=crop',
      memberCount: 23100,
      lastActive: 'Lần hoạt động gần nhất: 5 giờ trước',
      isJoined: true
    },
    {
      id: '4',
      name: 'Unix/Linux',
      description: 'Cộng đồng Unix và Linux Việt Nam',
      image: 'https://images.unsplash.com/photo-1629654297299-c8506221ca97?w=400&h=400&fit=crop',
      memberCount: 89400,
      lastActive: 'Đoạn chat đã tắt động',
      isJoined: true
    },
    {
      id: '5',
      name: 'Hỏi Đáp Thuật Toán',
      description: 'Nhóm hỏi đáp về thuật toán và lập trình',
      image: 'https://images.unsplash.com/photo-1515879218367-8466d910aaa4?w=400&h=400&fit=crop',
      memberCount: 12300,
      lastActive: 'Đoạn chat đã tắt động',
      isJoined: true
    }
  ]);

  const handleCreateGroup = (newGroup: Omit<Group, 'id' | 'lastActive' | 'isJoined'>) => {
    const group: Group = {
      ...newGroup,
      id: Date.now().toString(),
      lastActive: 'Vừa tạo',
      isJoined: true
    };
    setGroups([group, ...groups]);
    setSelectedGroup(group);
  };

  return (
    <div className="flex h-screen bg-[#f0f2f5]">
      
      <div className="flex-1 overflow-auto">
        {selectedGroup ? (
          <GroupFeed group={selectedGroup} />
        ) : (
          <div className="flex items-center justify-center h-full">
            <div className="text-center">
              <h2 className="mb-2">Chọn một nhóm để xem nội dung</h2>
              <p className="text-gray-600">
                Hoặc tạo một nhóm mới từ thanh bên trái
              </p>
            </div>
          </div>
        )}
      </div>

    </div>
  );
}

export default GroupPage;
