{
  "name": "smart_contract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "node": "npx hardhat node",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deployPost.js --network localhost",
    "deploy1": "npx hardhat run scripts/deployRelationship.js --network localhost",
    "deploy2": "npx hardhat run scripts/deployDonation.js --network localhost",
    "deploy3": "npx hardhat run scripts/deployAccount.js --network localhost",
    "deploy4": "npx hardhat run scripts/deployGroup.js --network localhost",
    "deployGr": "npx hardhat run scripts/deploy-group.js --network localhost",
    "deployAll": "npm run deploy && npm run deploy1 && npm run deploy2 && npm run deploy3 && npm run deploy4",
    "deploy:sepolia": "npx hardhat run scripts/deploy.js --network sepolia",
    "clean": "hardhat clean",
    "help": "echo '\n📋 Available Commands:\n  npm run node     - Start Hardhat local node\n  npm run compile  - Compile contracts\n  npm run deploy   - Deploy to localhost\n  npm run test     - Test gas-free transactions\n  npm run clean    - Clean artifacts\n'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@types/node": "^24.9.1",
    "hardhat": "^2.26.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
